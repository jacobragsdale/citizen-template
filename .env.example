# Copy to .env.dev and fill in, then: ln -sf .env.dev .env
# Run commands load it explicitly: uv run --env-file .env <cmd>
# Code reads os.environ only — no load_dotenv().

# Add one line per variable your app reads, with a placeholder value.
# EXAMPLE_API_KEY=changeme
